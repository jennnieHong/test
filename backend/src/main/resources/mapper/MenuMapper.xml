<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.menu.mapper.MenuMapper">

    <select id="findAll" resultType="com.example.menu.model.Menu">
        SELECT id, name, code, level, parent_id as parentId, device_type as deviceType
        FROM menus
        ORDER BY id ASC
    </select>

    <select id="findByLevel" resultType="com.example.menu.model.Menu">
        SELECT id, name, code, level, parent_id as parentId, device_type as deviceType
        FROM menus
        WHERE level = #{level}
        ORDER BY id ASC
    </select>

</mapper>
