// Duplicate registration controller removed to avoid ambiguous mappings.
// Kept for reference while developing; actual signup endpoint is implemented
// in AuthController. Remove this file if no longer needed.

package com.example.menu.controller;

import com.example.menu.repository.UserRepository;
import com.example.menu.model.User;

public class RegistrationController {
    // Intentionally left blank to avoid duplicate @RestController mappings
}
